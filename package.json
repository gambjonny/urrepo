{
  "name": "@urrepo/root",
  "version": "0.0.1",
  "description": "a basic monorepo setup",
  "scripts": {
    "build:all": "tsc -b",
    "test:all": "vitest run",
    "coverage:all": "vitest run --coverage",
    "format:check:all": "pnpm -r --no-bail run format:check",
    "lint:check:all": "pnpm -r --no-bail run lint:check"
  },
  "keywords": [],
  "author": "Jonny Gamba",
  "license": "ISC",
  "engines": {
    "node": "22.12.0",
    "pnpm": "10.0.0"
  },
  "volta": {
    "node": "22.12.0",
    "pnpm": "10.0.0"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "core-js"
    ]
  },
  "devDependencies": {
    "@eslint/js": "catalog:",
    "@pandacss/dev": "catalog:",
    "@types/node": "catalog:",
    "@vitest/coverage-v8": "^3.0.3",
    "@vitest/eslint-plugin": "catalog:",
    "@vitest/ui": "^3.0.3",
    "eslint": "catalog:",
    "eslint-config-prettier": "catalog:",
    "eslint-plugin-prettier": "catalog:",
    "prettier": "catalog:",
    "typescript": "catalog:",
    "vitest": "^3.0.3"
  }
}
